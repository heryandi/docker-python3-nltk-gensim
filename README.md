docker-python3-nltk-gensim
==========================

Dockerfile for image with python3, nltk, gensim installed

Use `docker pull heryandi/python3-nltk-gensim` to get the image built with this Dockerfile.
